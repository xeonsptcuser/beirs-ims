# ------------------------------------------------------------
# Local development settings
# Copy this file to .env and update values as needed
# ------------------------------------------------------------
APP_NAME=BEIRS
APP_ENV=local
APP_KEY=
APP_DEBUG=true
APP_URL=http://localhost:8000

# Frontend origin for CORS / Sanctum
FRONTEND_URL=http://localhost:5173
CORS_ALLOWED_ORIGINS=http://localhost:5173
SANCTUM_STATEFUL_DOMAINS=localhost:5173


LOG_CHANNEL=stack
LOG_LEVEL=debug

# Database (local Postgres)
DB_CONNECTION=pgsql
DB_HOST=127.0.0.1
DB_PORT=5432
DB_DATABASE=beirs_local
DB_USERNAME=postgres
DB_PASSWORD=postgres
DATABASE_URL=

# Queues / cache / sessions
CACHE_DRIVER=file
SESSION_DRIVER=file
SESSION_LIFETIME=120
QUEUE_CONNECTION=sync

# File storage
FILESYSTEM_DISK=public

OTP_ENABLED=false

# Supabase Storage (Project Settings > Storage > S3 Access)
SUPABASE_ACCESS_KEY_ID=
SUPABASE_SECRET_ACCESS_KEY=
SUPABASE_BUCKET=
SUPABASE_S3_ENDPOINT=

# for deployed application to

# Default = database
# QUEUE_CONNECTION=sync

# ------------------------------------------------------------
# Render deployment (backend + PostgreSQL)
# Uncomment and fill with the values from your Render dashboard
# ------------------------------------------------------------
# APP_ENV=production
# APP_DEBUG=false
# APP_URL=${RENDER_EXTERNAL_URL}
# FRONTEND_URL=https://<your-vercel-domain>
# CORS_ALLOWED_ORIGINS=${FRONTEND_URL}
# CORS_SUPPORTS_CREDENTIALS=true - since we are using withCridentails in frontend
# SANCTUM_STATEFUL_DOMAINS=<your-vercel-domain>
#
# DB_CONNECTION=pgsql
# DB_HOST=${PGHOST}
# DB_PORT=${PGPORT}
# DB_DATABASE=${PGDATABASE}
# DB_USERNAME=${PGUSER}
# DB_PASSWORD=${PGPASSWORD}
# DATABASE_URL=${DATABASE_URL}

# SESSION_DOMAIN=beirs-ims.onrender.com
# SESSION_SECURE_COOKIE=true
# SANCTUM_STATEFUL_DOMAINS=beirs-ims.vercel.app

# OTP_ENABLED=true
# CACHE_DRIVER=redis
# SESSION_DRIVER=redis
# QUEUE_CONNECTION=database
# FILESYSTEM_DISK=public
