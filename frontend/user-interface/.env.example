# ------------------------------------------------------------
# Local development (Vite)
# Copy to .env.local for local runs
# ------------------------------------------------------------
VITE_API_BASE_URL=http://localhost:8000
VITE_STORAGE_URL=http://localhost:8000/storage
# Keep this pointing to your API's /storage route (or CDN in front of it), even if the backend stores files in Supabase

# ------------------------------------------------------------
# Production deployment
# Configure these in your Render Static Site using your Render backend URL
# ------------------------------------------------------------
# VITE_API_BASE_URL=https://<your-render-backend>/api
# VITE_STORAGE_URL=https://<your-render-backend>/storage
