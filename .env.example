# Root environment selector used by tooling/CI to pick frontend/backend config
APP_ENVIRONMENT=local

# Relative paths to the environment files that should be used for this run.
# Switch APP_ENVIRONMENT to "deployed" to point to the production-ready envs.
BACKEND_ENV_FILE=backend/api/.env.${APP_ENVIRONMENT}
FRONTEND_ENV_FILE=frontend/user-interface/.env.${APP_ENVIRONMENT}
